# Dataset — challenge use only, no redistribution (see export/LICENSE)
export/
*.jsonl.tar.gz
*.jsonl
# derived results are safe to track
!results/*.jsonl

# Python
__pycache__/
*.py[cod]

# Local scratch
.venv/
venv/

# Tokenizer vocab, fetched once (3.6 MB)
.tokenizer_cache/

# Elicitation store: raw provider responses, derived from the export
analysis/complexity_model/store/

# Real API keys — see .env.example
.env

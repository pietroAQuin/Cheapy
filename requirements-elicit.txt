# Only needed to run the offline elicitation pipeline (analysis/complexity_model/
# run_pilot.py, test_single_request.py), never by the shipped router. Kept separate from
# requirements-dev.txt so capability_model.py's "no API keys required" guarantee has no
# accidental SDK dependency.
anthropic
openai
python-dotenv

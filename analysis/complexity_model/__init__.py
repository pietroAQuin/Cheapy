"""Capability model — pairwise-divergence scoring for the router.

See `complexity_scoring_spec.md` in this directory for the full design. The shipped
artifact is `capability_model.py`; everything else in this package is the offline
pipeline that produces it and runs exactly once.
"""

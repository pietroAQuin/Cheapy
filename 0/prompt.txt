lets work on the file price model. it should get a model list and a trajectory. then depending on the atributes of the trajectory and the atributes of each model (total tokens; price per token; price for cache...) it shoud then for each model come to a value beetween 0 and 100 and put it in its price score. how could we get to that score? keep in mind that the trajectory already uses a model that have tokens cached. changing the model is probably more expensive